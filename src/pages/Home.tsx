import {Box} from "@mui/material";

export function Home() {
    return (
        <Box sx={{
            color: "text.primary",
            bgcolor: "background.paper",
            minWidth: "100vw",
            minHeight: "100vh",
        }}>
            
        </Box>
    );
}